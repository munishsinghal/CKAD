
kind: Job
metadata:
  name: my-job
spec:
  completions: 3
  template:
    spec:
      containers:
      - name: my-container
        image: my-image
        command: ["echo", "Hello, World!"]
      restartPolicy: Never


----------------

see the outout of the pod
kubectl logs math-add-job-343djf
