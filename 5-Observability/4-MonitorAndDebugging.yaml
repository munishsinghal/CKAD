
Mertics Server is the only topic for this course. Earlier it was heapster. It is in-memory not disk storage for metrics.

minikube addons enable metrics-server

git clone https://github.com/kubernetes-incubator/metrics

kubectl create -f deploy/1.8+/
